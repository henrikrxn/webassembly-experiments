package henrikrxn:experiments

// This is an attempte at describing the "calculator" I use throughout this repository
interface calculator {
    add: func(x: u32, y: u32) -> u32
    subtract: func(x: u32, y: u32) -> u32
}

world implementor {
  export operations;
}

world consumer {
  import operations;
}