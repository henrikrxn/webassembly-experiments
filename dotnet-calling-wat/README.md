# Loading WAT in dotnet

Project file copies WAT calculator, `calculator.wat` file from folder in this repository.

Program loads the WAT file and calls a couple of functions in it.

## To build

```bash
dotnet build -c Debug
```

## To run

Makes a couple of WAT function calls and prints actual and expected result.

```bash
dotnet run -c Debug
```
